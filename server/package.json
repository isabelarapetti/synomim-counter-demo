{
  "name": "express-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "nodemon server.js",
    "docker:build": "docker build -t server:dev .",
    "docker:run": "docker run -d --name server -p 3000:3000 server:dev"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "mysql2": "^2.2.5",
    "node-wordnet": "^0.1.12",
    "sequelize": "^6.6.2",
    "wndb-with-exceptions": "^3.0.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
